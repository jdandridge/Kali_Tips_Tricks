# 9 Things To Do After Installing Kali Linux

## 1. Change Hostname
    $ whatis hostnamectl
    $ sudo hostnamectl set-hostname Demo
### Open nano and change (kali) to (Demo) in the /etc/hosts file
    $ sudo nano /etc/hosts # change (kali) to (Demo) in the hosts file
